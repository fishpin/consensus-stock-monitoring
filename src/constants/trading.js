// 0.15% half-spread applied to the mid price for simulated market orders
export const BID_MULTIPLIER = 0.9985;
export const ASK_MULTIPLIER = 1.0015;

// Spread warning threshold: show caution note when spread exceeds this percentage
export const WIDE_SPREAD_THRESHOLD_PCT = 1;

export const MIN_TRADE_QTY = 1;

export const NOTIFICATION_TIMEOUT_MS = 2500;
